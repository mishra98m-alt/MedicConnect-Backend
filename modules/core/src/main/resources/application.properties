# ==========================================================
# 🚀 MedicConnect Backend Configuration
# ==========================================================

# Server
server.port=8080
server.address=0.0.0.0

# Email (keep secrets out of repo in production)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=iam.marutmishra@gmail.com
spring.mail.password=kiruvfnqgqvpjwtg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Frontend base URL used in emails (use the actual frontend host:port)
frontend.base.url=http://10.5.48.251:8081

# JWT
jwt.auth.secret=F7A1C9D4B8E2F6A3D1C5B9E7A2F4D8C6B1E3A7D9C4F6B2A8E1C3D7B5A9F2E4
jwt.auth.expiration=86400000

jwt.prefilled.secret=A9C3E7F1B5D8A2C6E4F9B1D3A7C5E8F2B4D6A1F3C9B7E5A2C4F8D1E6B3A7
jwt.prefilled.expiration=3600000

# Logging & CORS
app.cors.allowed-origin=*
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

logging.level.org.springframework=INFO
logging.level.com.medicconnect=DEBUG
logging.level.feign=DEBUG

# ==========================================================
# OpenMRS REST + FHIR API (use docker service hostname when in compose)
# ==========================================================
openmrs.base-url=${OPENMRS_BASE_URL:http://openmrs:8080/openmrs/ws/rest/v1}
openmrs.fhir-base=${OPENMRS_FHIR_BASE:http://openmrs:8080/openmrs/ws/fhir2/R4}
openmrs.username=${OPENMRS_USERNAME:admin}
openmrs.password=${OPENMRS_PASSWORD:Admin123}

app.dev-mode=${APP_DEV_MODE:true}

# ==========================================================
# DB + Liquibase profile
# ==========================================================
spring.profiles.active=${SPRING_PROFILES_ACTIVE:openmrs}
spring.config.import=classpath:application-openmrs.properties


# Spring Boot minimal logs
logging.level.root=INFO
logging.level.org.springframework=INFO

# Only log MedicConnect code when something important happens
logging.level.com.medicconnect=INFO

# Feign client (OpenMRS) minimal logging
logging.level.feign=ERROR

spring.mvc.log-request-details=false

